{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Komentarze list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Idkomentarza</th>
                <th>Komentarz</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('komentarze_show', { 'id': entity.idKomentarza }) }}">{{ entity.idKomentarza }}</a></td>
                <td>{{ entity.komentarz }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('komentarze_show', { 'id': entity.idKomentarza }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('komentarze_edit', { 'id': entity.idKomentarza }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('komentarze_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
